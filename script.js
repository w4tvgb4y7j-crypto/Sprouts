let count = localStorage.getItem("progress") || 0;let theme = localStorage.getItem("theme") || "light";document.getElementById("progress").style.width = count + "%";setBadge();loadData();if (theme === "dark") {document.body.classList.add("dark");}// Add Item Functionfunction addItem(type) {let input, list;if (type === "goal") {input = goalInput;list = goalList;} else if (type === "skill") {input = skillInput;list = skillList;} else {input = achieveInput;list = achieveList;}if (!input.value) return;const li = document.createElement("li");li.innerText = input.value;list.appendChild(li);saveData(type, input.value);input.value = "";updateProgress();}// Progress & Badgefunction updateProgress() {count = parseInt(count) + 5;if (count > 100) count = 100;document.getElementById("progress").style.width = count + "%";localStorage.setItem("progress", count);setBadge();}function setBadge() {const badge = document.getElementById("badge");if (count < 25) badge.innerText = "  Starter";else if (count < 50) badge.innerText = "  Builder";else if (count < 75) badge.innerText = "  Achiever";else badge.innerText = "  Life Architect";}// Theme Togglefunction toggleTheme() {document.body.classList.toggle("dark");theme = document.body.classList.contains("dark") ? "dark" : "light";localStorage.setItem("theme", theme);}// Save & Loadfunction saveData(type, value) {let data = JSON.parse(localStorage.getItem(type) || "[]");data.push(value);localStorage.setItem(type, JSON.stringify(data));}function loadData() {["goal", "skill", "achieve"].forEach(type => {let data = JSON.parse(localStorage.getItem(type) || "[]");const list = document.getElementById(type + "List");data.forEach(item => {const li = document.createElement("li");li.innerText = item;list.appendChild(li);});});}// Export as PDFfunction downloadPDF() {window.print();}
